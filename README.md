## Setup

### Environment

Requires Python 3.11+

```bash
python3.14 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt


5May26 Created clean text func for data ingest, with unicode and whitespace functionality. Created unit tests for clean text func. Created gitignore file to stop pycache files from being pushed. Pip installed pytest, ran first pytest. running pythonpath=src for user modules. 

4May26: Setup and env set up, github project setup. Connected repo to local machin. Created dummy data and ingestion script using json object.

